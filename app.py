from gpu_job import run_gpu_job

def main():
    print("Submitting GPU job to AIDP-compatible interface...")
    run_gpu_job()
    print("Job finished. Ready for AIDP alpha integration.")

if __name__ == "__main__":
    main()
